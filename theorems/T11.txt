T11 — CAMPO DA ESCOLHA RACIONAL (revive §13–§15 com a definição de liberdade)

Depende de: T5, T9; definições §14–§15; definição de sujeito (origem)
(definitional-subject 2026-09-17)

Verificado em: formal/Logos/Choice.lean (T11_choiceField, person_chooses,
choiceExists, noChoice_selfRefutes, JUDGE_COMMITTED,
rightWrong_implies_someone_means, meaning_I_needs_subject,
freeWillOrigin, noFreeWillPosited, originFreedomSelfRefutes, judgeIsFree),
formal/Logos/Order.lean (judge_commits, rightWrong_implies_meaning,
rightDistinctWrong_implies_meaning, rightWrongDistinction_implies_meaning),
GAPMAP.md C39–C72.
Impressão de axiomas: T11_choiceField — `{}`; a de T5 (`{}`, definitional-subject);
person_chooses — `{}` (PROVEN);
rightWrong_implies_meaning / rightDistinctWrong_implies_meaning — `{}`
(PROVEN, C62, sem axioma substantivo);
meaning_I_needs_subject — `{}` (PROVEN, C49);
choiceExists / noChoice_selfRefutes / JUDGE_COMMITTED — `{}`;
judge_commits / rightWrongDistinction_implies_meaning — `CL`;
incompatible_self_negation — nenhuma ({}); sorryAx: 0.
freeWillOrigin — `{}` (C69); noFreeWillPosited — `CL` (C70);
originFreedomSelfRefutes — `{}` (C71); judgeIsFree — `{}` (C72).

Definições §8 (2026-09-16, versão literal Correct(A(s,p))):
Correct(s,p)   := A(s,p) ∧ T(p);
Incorrect(s,p) := A(s,p) ∧ IsFalse(p);
Meaning_I(p)   := ∃s Means(s,p).

Enunciado:

Há uma pessoa (T5) e há conteúdos incompatíveis (T9); logo o campo mínimo da
escolha racional (§13–§14) está não-vazio para essa pessoa:

∃s Person(s) ∧ ∃p∃q Incompatible(p,q).

Definições formalizadas (batch choice-realism, 2026-09-16):

Chooses(s,p,q)   := A(s,p) ∧ Incompatible(p,q)  — o sujeito s adota p contra o
                    conteúdo incompatível q (§14); ESTA É AGORA UMA DEFINIÇÃO
                    REAL (antes era o placeholder vazio ":= False", que tornava
                    a escolha irrepresentável — os "gaps" abaixo eram falsos).
CanChoose(s,p)   := Dia(∃q Chooses(s,p,q))
FreeWill(s,p)    := CanChoose(s,p) ∧ CanChoose(s,¬p)        [§15, definição]

Teoremas kernel-verificados (a cadeia §2 de IM_STUPID.md fecha para a
existência da escolha):

- person_chooses: Person(s) ⇒ ∃p∃q Chooses(s,p,q) —  sujeito ⇒ escolha.
- rightWrong_implies_meaning / rightDistinctWrong_implies_meaning (§8/poem P3):
  (∃s∃p Correct(s,p)) ∨/∧ (∃s∃p Incorrect(s,p)) ⇒ ∃p Meaning_I(p) — "direito e
  errado precisam de significado": Correct/Incorrect são atos de significar
  (A(s,p) ∧ …), logo a negação é contradição por definição, em qualquer modelo,
  inclusive o vazio. `{}` (C62); nada substantivo.
- rightWrongDistinction_implies_meaning: a forma completa do condicional poético
  (¬N_T ∧ ¬N_F ⇒ ∃p Meaning_I(p)); `CL`.
- rightWrong_implies_someone_means (§25/poem P3): ¬N_T ∧ ¬N_F ⇒ ∃s∃p Means(s,p)
  — "há certo e errado ⇒ há alguém para quem algo significar" (C61,
  `{}`); a metade "significado ⇒ sujeito" é analítica
  meaning_needs_subject / meaning_I_needs_subject (C49, `{}`).
- choiceExists: ∃s∃p∃q Chooses(s,p,q) —  a escolha é real (via T5).
- noChoice_selfRefutes: ¬(∃s∃p∃q Chooses(s,p,q)) ⇒ False —  negar a escolha
  é uma escolha (transcendental).
- JUDGE_COMMITTED e judge_commits: sem certo e errado não há escolha —
  o juiz (§8) é um escolhedor.

FREEDOM — F1b SPLIT (2026-09-17; o paradoxo "não pude escolher de outro modo"
dissolve-se):

F1b-weak — PROVEN. A origem do ato presente (o sujeito do cogito, a testemunha
de T5 a que JUDGE_COMMITTED compromete o certo-e-errado) pode escolher os dois
lados:

- freeWillOrigin: FreeWill(inl(),p) — `{}` (a origem significa todo o
  conteúdo: Initiates(inl _, w, w', p) := w' = p; CanChoose por instanciação
  em someWorld, sem o canChoose_unfold clássico). C69.
- originFreedomSelfRefutes: ¬FreeWill(inl(),p) ⇒ False — "não pude escolher
  de outro modo" auto-refuta-se: a negação é ela própria um ato da origem
  livre. C71.
- judgeIsFree: ∃s Person(s) ∧ ∃p FreeWill(s,p) — a escolha-no-sentido-da-
  liberdade existe para o sujeito que o certo/errado inegável compromete. C72.
- noFreeWillPosited: ¬FreeWill(inr q,p) — `CL`: os sujeitos postos
  provadamente NÃO podem escolher de outro modo (só significam q próprio;
  bilateralidade forçaria q = p ∧ q = ¬p, impossível). C70 — sem paradoxo:
  nunca são o juiz a que o certo/errado se compromete.

F1b-strong — BLOCKED (gap de VOCABULÁRIO, não de prova): a alternatividade a
nível-mundos, ◇PH Choose ∧ ◇PH Choose¬ sobre NecessityPH, exige um predicado
de escolha genuinamente variável por mundo — ChoiceAt : World → Subject → Prop
→ Prop. Todo o predicado atual do sistema é invariante ao mundo, logo
NecessityPH aplicado a qualquer um colapsa em identidade; a ponte (SEM/META)
é deliberadamente adiada. CanChoose já é escolha real (canChoose_unfold) e a
bilateralidade do sujeito do ato está demonstrada — o que faltaria a F1b-strong
não é hoje formulável, não apenas não provado (diferença do BLOCKED de C19).
GAPMAP.md F1b-weak/F1b-strong, C69–C72.