T11 — CAMPO DA ESCOLHA RACIONAL (revive §13–§15 com a definição de liberdade)

Depende de: T5, T9; definições §14–§15 (desacoplado de `AxTwoSubjects` na existência do campo e retorsão)

Verificado em: formal/Logos/Choice.lean (T11_choiceField, person_hasChoiceField,
choiceField_exists, noChoiceField_selfRefutes, JUDGE_HAS_CHOICE_FIELD,
chooses_implies_freeWill, rightWrong_implies_someone_means,
meaning_I_needs_subject, rejectedHornCoMeant, rejectedHornCoMeant_implies_genuineChoice,
genuineChoice_requires_error_possibility, assertion_consistency,
no_one_asserts_incompatible_pair), formal/Logos/Order.lean (judge_commits,
rightWrong_implies_meaning, rightDistinctWrong_implies_meaning,
rightWrongDistinction_implies_meaning), formal/Logos/HostileSemantics.lean
(CountermodelNoFreeWill, not_entails_decoupled_freewill,
CountermodelVeridicalMeaning, not_entails_genuine_choice,
not_entails_genuine_choice_with_plurality), GAPMAP.md C39–C72/F1a/F1b.
Impressão de axiomas:
T11_choiceField — `{Means, Subject}` (herda de T5; C39 desacoplado de AxTwoSubjects);
person_hasChoiceField — `{Means, Subject}` (PROVEN, campo de escolha, NÃO escolha genuína;
  renomeado de person_chooses, 2026-09-18);
rejectedHornCoMeant_implies_genuineChoice, genuineChoice_requires_error_possibility,
assertion_consistency, no_one_asserts_incompatible_pair — `{Means, Subject}`
(novos, milestone hostil 2026-09-18); hostile separations/no_genuine_choice — nenhuma ({});
rightWrong_implies_meaning / rightDistinctWrong_implies_meaning — `{Means, Subject}`
(PROVEN, C62, só vocabulário);
meaning_I_needs_subject — `{Means, Subject}` (PROVEN, C49);
choiceField_exists — `{Means, Subject}` (PROVEN, C52, derivado de C68);
noChoiceField_selfRefutes — `{Means, Subject}` (PROVEN, C53, retorsão performativa do campo);
JUDGE_HAS_CHOICE_FIELD — `{AxTwoSubjects, Means, Subject}` (PROVEN↑, C54, via AxTwoSubjects h);
judge_commits / rightWrongDistinction_implies_meaning — `CL + {AxTwoSubjects, Means, Subject}`;
chooses_implies_freeWill — `{Means, Subject}` (só vocabulário), por definição;
incompatible_self_negation — nenhuma ({}); sorryAx: 0.

Definições §8 (2026-09-16, versão literal Correct(A(s,p))):
Correct(s,p)   := A(s,p) ∧ T(p);
Incorrect(s,p) := A(s,p) ∧ IsFalse(p);
Meaning_I(p)   := ∃s Means(s,p).

Enunciado:

Há uma pessoa (T5) e há conteúdos incompatíveis (T9); logo o campo mínimo da
escolha racional (§13–§14) está não-vazio para essa pessoa:

∃s Person(s) ∧ ∃p∃q Incompatible(p,q).

Definições formalizadas (reparadas 2026-09-18 — freedom/choice fix):

ChoiceField(s,p,q) := A(s,p) ∧ Incompatible(p,q)  — o CAMPO de escolha: o
                    agente é posto contra um par incompatível, mas relaciona-se
                    só com p (o corno ¬p vem da lógica pura). É a antiga relação
                    `Chooses`; NÃO é escolha.
Chooses(s,p,q)   := A(s,p) ∧ A(s,q) ∧ Incompatible(p,q)  — ESCOLHA GENUÍNA: o
                    agente co-significa os dois cornos incompatíveis (§14).
                    Antes (2026-09-16) o corpo era só `A(s,p) ∧ Incompatible(p,q)`,
                    que colapsava em A(s,p) — ocorrência determinada, não escolha.
CanChoose(s,p)   := Dia(∃q Chooses(s,p,q))
FreeWill(s)      := ∃p∃q Chooses(s,p,q)          [§15, DEFINIÇÃO: escolha → liberdade]
chooses_implies_freeWill : Chooses(s,p,q) ⇒ FreeWill(s)   [{Means, Subject} só vocab — por definição]

O passo EM FALTA (F1b, BLOCKED) é a existência da escolha genuína:

    rejectedHornCoMeant : ∃s p, A(s,p) ∧ A(s,¬p)

(formalizado como def-proposição, nó BLOCKED.) Nada força um ato de significar
p a vir com a significação de ¬p: `Means` é opaco e AxTwoSubjects dá dois
sujeitos diferentes, cada um com um só conteúdo. `Chooses → FreeWill` é
definicional; o que falta é o escolhedor. (Forma condicional em prosa:
(∃s p, A(s,p)) → ∃s p, A(s,p) ∧ A(s,¬p).)

Teoremas kernel-verificados (a cadeia §2 de IM_STUPID.md fecha para a
existência do CAMPO de escolha; a escolha genuína é F1b, bloqueada):

- person_hasChoiceField: Person(s) ⇒ ∃p∃q ChoiceField(s,p,q) — sujeito ⇒ campo.
- rightWrong_implies_meaning / rightDistinctWrong_implies_meaning (§8/poem P3):
  (∃s∃p Correct(s,p)) ∨/∧ (∃s∃p Incorrect(s,p)) ⇒ ∃p Meaning_I(p) — "direito e
  errado precisam de significado": Correct/Incorrect são atos de significar
  (A(s,p) ∧ …), logo a negação é contradição por definição, em qualquer modelo,
  inclusive o vazio. `{}` (C62); nada substantivo.
- rightWrongDistinction_implies_meaning: a forma completa do condicional poético
  (¬N_T ∧ ¬N_F ⇒ ∃p Meaning_I(p)); `CL`.
- rightWrong_implies_someone_means (§25/poem P3): ¬N_T ∧ ¬N_F ⇒ ∃s∃p Means(s,p)
  — "há certo e errado ⇒ há alguém para quem algo significar" (C61,
  `{}`); a metade "significado ⇒ sujeito" é analítica
  meaning_needs_subject / meaning_I_needs_subject (C49, `{}`).
- choiceField_exists: ∃s∃p∃q ChoiceField(s,p,q) — o campo é real (via T5).
- noChoiceField_selfRefutes: ¬(∃s∃p∃q ChoiceField(s,p,q)) ⇒ False — negar o
  campo é um ato de campo (transcendental).
- JUDGE_HAS_CHOICE_FIELD e judge_commits: sem certo e errado não há campo de
  escolha — o juiz (§8) tem um campo (a escolha genuína é bloqueada).

FREEDOM — F1 (reparado 2026-09-18; semântica hostil e alternatividade):
CLASSIFICAÇÃO ONTOLÓGICA DAS PASSAGENS:
- Person → ChoiceField: DEFINICIONAL/analítica (person_hasChoiceField, {Means, Subject}).
- ChoiceField ↛ Chooses: BLOQUEADO (F1b) no lema exacto `rejectedHornCoMeant`
  — a Agência hostil mostra que um ato (e mesmo a pessoa) não força a
  co-significação do corno rejeitado.
- Chooses → FreeWill: DEFINICIONAL (chooses_implies_freeWill, `{Means, Subject}`
  só vocabulário). A liberdade
  não é um passo metafísico a partir do ato bruto; é a própria existência de
  uma escolha genuína. O antigo contramodelo `CountermodelNoFreeWill` /
  `not_entails_decoupled_freewill` (em `formal/Logos/HostileSemantics.lean`,
  com `Chooses := False`) mostra `Act ↛ Chooses` e `Act ↛ FreeWill` — NÃO é
  contramodelo da definição `Chooses → FreeWill`.
- O campo (`ChoiceField`, person_hasChoiceField, choiceField_exists) e o juiz
  que o certo-e-errado compromete (`JUDGE_HAS_CHOICE_FIELD`) são teoremas; o
  que fica BLOCKED é a *existência* de um escolhedor genuíno.
- Veredito hostil 2026-09-18 (opção 3 — bloqueio irreducível sob os primitivos
  `{Means, Subject}`): `genuineChoice_requires_error_possibility`
  (veridicalidade mata co-significação), `assertion_consistency` /
  `no_one_asserts_incompatible_pair` (assertir é de um-corno-só por definição),
  `rejectedHornCoMeant_implies_genuineChoice` (fronteira mínima), e
  `CountermodelVeridicalMeaning` + `not_entails_genuine_choice(_with_plurality)`
  (`{}` — datum, pluralidade, certo/errado, judge_commits e falibilidade valem
  com escolha vazia). Premissas substantivas candidatas (`AxCoMeaningNegation`,
  co-sujeito `Incorrect∧Denies`, auto-representação) registadas, NÃO
  introduzidas.
- O nível-mundos (F7, `ChoiceAt : World → Subject → Prop → Prop`) permanece a
  ponte SEM futura, secundária. Ver formal/GAPMAP.md F1a/F1b.